
@{
    ViewBag.Title = "Index";
}

<div class="container">

    <div class="row align-items-center my-1">

        <div class="col-6">

            <h1 class="display-6">Rentals</h1>

        </div>

        <div class="col-6 text-end">

            @if (User.Identity.IsAuthenticated)
            {
                @Html.ActionLink("Add Rental", "Add", null, new { @class = "btn btn-sm btn-primary" })
            }

        </div>

    </div>

</div>
